function main() {
  // Write your code here. Read input using 'readLine()' and print output using 'console.log()'.

  const r =readLine();    
  const PI = Math.PI;
  
  // Print the area of the circle:
  const area = (PI * (r * r)); 
   console.log(area);
  // Print the perimeter of the circle:
  const perimetar = (2 * PI * r);
  console.log(perimetar);
}